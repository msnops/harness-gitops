# kustomize/base/traefik/kustomization.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yaml
  - helmrelease.yaml
  - ingressclass.yaml

commonLabels:
  app.kubernetes.io/managed-by: harness-gitops
  app.kubernetes.io/component: ingress-controller
  ingress-type: traefik
