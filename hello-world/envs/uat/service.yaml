apiVersion: v1
kind: Service
metadata:
  name: hello-app
  namespace: uat
spec:
  type: NodePort
  selector:
    app: hello-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30002   # UAT: access via localhost:30002