apiVersion: v1
kind: Service
metadata:
  name: hello-app
  namespace: prod
spec:
  type: NodePort
  selector:
    app: hello-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30003   # PROD: access via localhost:30003